name: 🧾 Story
description: Define a user story for the project
title: "[Story]: "
labels: ["story"]
body:
  - type: markdown
    attributes:
      value: |
        Please provide a user story that describes the functionality from the user's perspective. Use the format: "As a [type of user], I want [some goal] so that [some reason]."
  - type: input
    id: user-story
    attributes:
      label: User Story
      placeholder: "As a [type of user], I want [some goal] so that [some reason]."
    validations:
      required: true